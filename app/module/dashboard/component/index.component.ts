import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'dashboard-index',
    template: `<h1>Welcome to paycom dashboard!!!</h1>`,
})
export class IndexComponent {
}